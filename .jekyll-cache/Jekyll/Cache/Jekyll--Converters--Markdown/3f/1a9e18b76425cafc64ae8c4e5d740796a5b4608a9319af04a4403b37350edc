I"�<h1 id="postman-调试-https-接口时去掉证书验证">Postman 调试 HTTPS 接口时去掉证书验证</h1>

<p>如果机器对外有安全性要求，只能提供 HTTPS，同时还没绑定域名，使用 Postman 进行接口调试时，通过 IP 去访问机器上的服务是会报证书错误的：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET https://&lt;ip_of_my_server&gt;:443/api/endpoint

Error: Hostname/IP does not match certificate's altnames: IP: &lt;ip_of_my_server&gt; is not in the cert's list:

</code></pre></div></div>

<p>在接口的设置界面关闭 <code class="language-plaintext highlighter-rouge">Enable SSL certificate verification</code> 选项即可绕开该报错。</p>

<p><img src="https://user-images.githubusercontent.com/3783096/92689847-61bfc300-f372-11ea-80d4-0b58bf975666.png" alt="image" /></p>
<p align="center">关闭 Postman 接口的 SSL 证书检查</p>
:ET